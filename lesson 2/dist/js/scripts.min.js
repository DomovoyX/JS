let money = +prompt("Ваш бюджет?", "");
let name	= prompt("Название вашего магазина?", "");
let time = 19;

let mainList = {
	money:money,
	moneyOneDay:money/30+" руб.",
	shopName:name,
	shopGoods:[],
	employers:{
		associate1:"Семён",
		associate2:"Игорь",
		associate3:"Кирилл",
		associate4:"Андрей"
	},
	statusOpen:"Открыт",
	statusClose:"Закрыт"
};

/*
for (let i = 0; i < 5; i++) {
	let a = prompt("Какой тип товаров будем продавать?", "");
	if ((typeof(a)) === 'string' && (typeof(a)) !== 'null' && a != '' && a.length < 50){
		console.log('верно');
		mainList.shopGoods[i] = a;
	} else {
		console.log('не верно');
		mainList.shopGoods[i] = 'error';
	}
}*/



//ввод осуществиться 5 раз, при не верном вводе запишится ошибка
let i = 0;
while (i<5){
let a = prompt("Какой тип товаров будем продавать?", "");
	if ((typeof(a)) === 'string' && (typeof(a)) !== 'null' && a != '' && a.length < 50) {
		console.log('верно');
		mainList.shopGoods[i] = a;
	} else {
		console.log('не верно');
		mainList.shopGoods[i] = 'error';
	}
		i++;
}
/*
//ввод будет осуществлять пака не будет введено 5 значений удовлетворяющих условию
let i = 0;
while (i<5){
let a = prompt("Какой тип товаров будем продавать?", "");
	if ((typeof(a)) === 'string' && (typeof(a)) !== 'null' && a != '' && a.length < 50) {
		console.log('верно');
		mainList.shopGoods[i] = a;
		i++;
	} else {
		console.log('не верно');
	}
}*/

/*
let i = 0;
do {
	let a = prompt("Какой тип товаров будем продавать?", "");
	if ((typeof(a)) === 'string' && (typeof(a)) !== 'null' && a != '' && a.length < 50) {
		console.log('верно');
		mainList.shopGoods[i] = a;
	} else if ( a == ''){
		console.log('не верно');
		mainList.shopGoods[i] = 'empty_value';
	} else{
		console.log('не верно');
		mainList.shopGoods[i] = 'error';
	}
	i++;
}
while (i < 5);*/

console.log(mainList);




